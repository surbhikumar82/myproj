using namespace std;
int fact( int );

int main() {
  int n;
  printf( "enter the number");
  //scanf( "%d", &n);
  //printf("Factorial of " %d " = "%ld",fact(n));
  //fact(5);
  printf("factorail is %d", fact(5) );
  return 0;
     
}


/* 
Factorial 
!5 = 5*4*3*2*1 

fact(5) == 120

*/

int fact( int n)
{
  if(n>=1)
  
    return (n * fact( n-1));
 
 else
   return 1;

}
